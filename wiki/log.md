---
title: Wiki Log
type: log
created: 2026-05-15
updated: 2026-05-17
---

# Wiki Log

Append-only chronological log of operations on the wiki. Each entry starts with `## [YYYY-MM-DD] {verb} | {short title}` so it's grep-friendly:

```bash
grep "^## \[" wiki/log.md | tail -5     # last 5 entries
grep "^## \[.*\] ingest" wiki/log.md    # all ingests
```

Verbs: `ingest`, `query`, `lint`, `quiz`, `refactor`.

---

## [2026-05-15] init | wiki scaffolded

- **What:** Created CLAUDE.md schema, three project skills (wiki-ingest, wiki-query, wiki-lint), and wiki/ directory structure (concepts, methods, topics, sources, questions). Initialised empty index.md and this log.
- **Pages touched:** [[index]], [[log]]
- **Notes:** Vault is empty — ingest a first source to populate.

## [2026-05-15] ingest | Few-step Generative Models (Flow-Map models) lecture

- **What:** First content ingest. A 31-slide lecture on why diffusion is slow and the modern toolkit of flow-map methods (Consistency Models, Multistep CMs, ShortCut, Mean Flow). Sourced from `raw/lectures/flow-map-models.pdf`.
- **Pages touched:** [[sources/flow-map-models-lecture]] (new), [[topics/few-step-generative-models]] (new, draft), [[ml_concepts/flow-map]] (new, draft), [[ml_concepts/consistency-function]] (new, draft), [[ml_concepts/step-distillation]] (new, draft), [[ml_concepts/diffusion-model]] (new, stub), [[ml_concepts/flow-matching]] (new, stub), [[ml_concepts/probability-flow-ode]] (new, stub), [[ml_concepts/score-function]] (new, stub), [[methods/consistency-distillation]] (new, draft), [[methods/consistency-training]] (new, draft), [[methods/multistep-consistency-model]] (new, draft), [[methods/shortcut-model]] (new, draft), [[methods/mean-flow]] (new, draft), [[methods/progressive-distillation]] (new, stub), [[math_concepts/mean-flow-identity]] (new, draft), [[index]] (updated).
- **Notes:** Flow map is the unifying concept across the whole lecture; raised three open questions ([[questions/why-cant-cms-use-ode-solvers]], [[questions/how-is-mean-flow-time-derivative-computed]], [[questions/why-does-consistency-training-work-without-teacher]]). Background pages (diffusion-model, flow-matching, probability-flow-ode, score-function) deliberately left as stubs — a dedicated source will fill them. Final slide ("Айтишники всё!") is a meme sign-off, not technical content.

## [2026-05-15] ingest | ELBO and VAE lecture

- **What:** Lecture deriving ELBO from first principles (Jensen and Bayes routes), decomposing into reconstruction + regularisation, and walking through the full VAE training story (variational EM, amortised inference, reparameterization trick, closed-form Gaussian KL, final loss). Sourced from `raw/lectures/ELBO_and_VAE.md`.
- **Pages touched:** [[sources/elbo-and-vae-lecture]] (new), [[ml_concepts/elbo]] (new, draft), [[ml_concepts/latent-variable-model]] (new, draft), [[ml_concepts/variational-inference]] (new, draft), [[ml_concepts/amortized-variational-inference]] (new, draft), [[ml_concepts/reparameterization-trick]] (new, draft), [[math_concepts/kl-divergence]] (new, draft), [[math_concepts/jensens-inequality]] (new, draft), [[methods/vae]] (new, draft), [[methods/variational-em]] (new, draft), [[topics/variational-inference]] (new, draft), [[index]] (updated).
- **Notes:** Opens the variational-inference region of the wiki, disjoint from the existing flow-map / consistency-models cluster. The score-function estimator is mentioned only briefly on [[ml_concepts/reparameterization-trick]] and not given its own page — a dedicated source could expand it later. Topic page links to concept page via explicit `[[ml_concepts/variational-inference]]` to disambiguate from `[[topics/variational-inference]]`. No contradictions with existing pages.

## [2026-05-15] refactor | Narrative layer — topic primers, Motivation, Up next

- **What:** Schema and content refactor to add a narrative layer on top of the existing reference layer, addressing two problems: (1) no entry point for "study an area from scratch", (2) `## Intuition` sections jumped to formal answers without naming the motivating question.
- **Schema (`CLAUDE.md`):** new topic template (primer form: The setting → Core ideas → Methods → Open threads → Reading order recap → Reading queue); ml_concept and method templates get `## Motivation` (replaces Intuition on ml_concept; new section on method) and `## Up next` footer; math_concept template's `## Plain-English statement` absorbs motivated build-up; Quality bar gains `### Motivation voice` and `### Up next footer` subsections; banlist extended with bolded Q&A markers and everyday-object metaphors. Topic page taxonomy row updated from "umbrella link map" to "narrative primer".
- **Skills:** [[.claude/skills/wiki-ingest/SKILL.md]] (Intuition reference updated); [[.claude/skills/wiki-lint/SKILL.md]] (Check 7 extended with Q&A markers and metaphors; new Check 7b — schema migration grep for residual `## Intuition`).
- **Topic primers (rewritten):** [[topics/variational-inference]], [[topics/few-step-generative-models]].
- **ml_concept Motivation + Up next:** [[ml_concepts/elbo]], [[ml_concepts/amortized-variational-inference]], [[ml_concepts/consistency-function]], [[ml_concepts/flow-map]], [[ml_concepts/latent-variable-model]], [[ml_concepts/reparameterization-trick]], [[ml_concepts/step-distillation]], [[ml_concepts/variational-inference]]. Stubs left as-is (no Intuition section to migrate): [[ml_concepts/diffusion-model]], [[ml_concepts/flow-matching]], [[ml_concepts/probability-flow-ode]], [[ml_concepts/score-function]].
- **math_concept Plain-English rewritten:** [[math_concepts/jensens-inequality]], [[math_concepts/kl-divergence]], [[math_concepts/mean-flow-identity]].
- **method Motivation + Up next:** [[methods/consistency-distillation]], [[methods/consistency-training]], [[methods/mean-flow]], [[methods/multistep-consistency-model]], [[methods/shortcut-model]], [[methods/vae]], [[methods/variational-em]]. Stub [[methods/progressive-distillation]] left as-is.
- **Bookkeeping:** [[index]] gains a `## Start here` section pointing at the two primers with their reading paths; alphabetical catalog below unchanged.
- **Voice contract:** all new Motivation sections, primer bodies, and math Plain-English rewrites follow motivated build-up — name what we want → name the naive thing → name why it fails → name the workaround. No Q&A markers, no metaphors, no marketing voice. Reference paragraph is the Motivation on [[ml_concepts/elbo]].
- **Notes:** Voice quality enforced by `wiki-lint` Check 7 going forward. Stubs will get Motivation and Up next when their ingest sources arrive.

## [2026-05-17] ingest | RoPE — full lecture (position encoding, derivation, 2D/3D, context extension)

- **What:** Five-part lecture covering the full positional-encoding landscape: (1) why self-attention needs PE and the limits of sinusoidal/learned-absolute schemes; (2) geometric derivation of RoPE in 2D via rotation matrices; (3) $d$-dim algorithm with the $\theta_i = 10000^{-2i/d}$ frequency schedule and block-diagonal structure; (4) 2D (ViT) and 3D (video) generalisations by distributing pairs across spatial/spatiotemporal axes; (5) context-extension methods — PI, NTK-Aware, YaRN, and DyPE. Sourced from `raw/lectures/RoPE/RoPE.md`.
- **Pages touched:** [[sources/rope-lecture]] (new, draft), [[topics/positional-encoding]] (new, draft), [[ml_concepts/positional-encoding]] (new, draft), [[ml_concepts/rotary-position-embedding]] (new, draft), [[ml_concepts/self-attention]] (new, stub), [[math_concepts/rotation-matrix-2d]] (new, draft), [[methods/sinusoidal-position-encoding]] (new, draft), [[methods/learned-absolute-position-embedding]] (new, draft), [[methods/rope]] (new, draft), [[methods/position-interpolation]] (new, draft), [[methods/ntk-aware-interpolation]] (new, draft), [[methods/yarn]] (new, draft), [[methods/dype]] (new, draft), [[index]] (updated).
- **Notes:** Opens the attention/transformer region of the wiki — disjoint from existing variational-inference and few-step-generative-models clusters; no contradictions. `self-attention` left as a stub on purpose — the lecture only uses it to motivate PE, so a dedicated source will fill it later. Reading queue on [[topics/positional-encoding]] lists primary sources for each method (Vaswani 2017, RoFormer 2021, PI 2023, NTK blog 2023, YaRN 2023, RoPE-ViT 2024, DyPE TBD) as natural next ingests. Used 9 of 11 lecture images embedded inline via vault-relative paths; two duplicate frames skipped (rationale on the source page). The lecture didn't formulate explicit open questions, so no `questions/` pages were created — minor sub-questions live in the "Open questions" sections of concept pages and can be promoted later.

## [2026-05-15] refactor | Phase B — narrative pages translated to Russian

- **What:** Translated narrative prose of all 30 wiki pages (questions, sources, math_concepts, topics, ml_concepts, methods) from English to Russian per the Phase A language policy. Section headings, frontmatter `title:`, H1, slugs, tags, `[[wiki-links]]`, ML/math terms, LaTeX math, code blocks, and backticked identifiers remain English.
- **Order:** questions (3) → sources (2) → math_concepts (3) → topics (2) → ml_concepts (12, including `elbo.md` which was still English despite the audit's claim) → methods (8). Total ≈ 2200 lines of prose.
- **Pages touched:** [[questions/how-is-mean-flow-time-derivative-computed]], [[questions/why-cant-cms-use-ode-solvers]], [[questions/why-does-consistency-training-work-without-teacher]], [[sources/elbo-and-vae-lecture]], [[sources/flow-map-models-lecture]], [[math_concepts/jensens-inequality]], [[math_concepts/kl-divergence]], [[math_concepts/mean-flow-identity]], [[topics/few-step-generative-models]], [[topics/variational-inference]], [[ml_concepts/amortized-variational-inference]], [[ml_concepts/consistency-function]], [[ml_concepts/diffusion-model]], [[ml_concepts/elbo]], [[ml_concepts/flow-map]], [[ml_concepts/flow-matching]], [[ml_concepts/latent-variable-model]], [[ml_concepts/probability-flow-ode]], [[ml_concepts/reparameterization-trick]], [[ml_concepts/score-function]], [[ml_concepts/step-distillation]], [[ml_concepts/variational-inference]], [[methods/consistency-distillation]], [[methods/consistency-training]], [[methods/mean-flow]], [[methods/multistep-consistency-model]], [[methods/progressive-distillation]], [[methods/shortcut-model]], [[methods/vae]], [[methods/variational-em]].
- **Verification:** grep for residual English narrative tokens (however, therefore, we want, note that, consider, recall, etc.) returned only one match in [[methods/multistep-consistency-model]] — a direct quoted lecture excerpt in guillemets, correct to leave English. All other matches are in section headings, LaTeX `\text{}`, bolded thematic sub-labels (e.g. **Exact identity with the posterior gap.**), or English ML/math terms — all allowed by the policy.
- **Not touched:** `updated:` field in frontmatter (decision: keep history clean since the change is language-only, not content); [[index]] (service file, English); old log entries (append-only); `CLAUDE.md` and `.claude/skills/**` (service text, English); `raw/` (immutable).

## [2026-05-17] ingest | The Illustrated Transformer (Jay Alammar)

- **What:** Visual walk-through of Vaswani 2017 Transformer architecture: encoder/decoder stacks, self-attention step by step (Q/K/V from embedding, scaled dot-product, softmax, weighted sum, matrix form), multi-head attention (8 heads, $d_k=64$, concat + $W^O$), sinusoidal positional encoding, residual + LayerNorm wraps around every sublayer, decoder side (masked self-attention + encoder-decoder cross-attention), final linear + softmax, cross-entropy training, greedy vs beam search decoding. Sourced from `raw/clips/The Illustrated Transformer.md`.
- **Pages touched:** [[sources/illustrated-transformer]] (new, draft), [[ml_concepts/self-attention]] (upgraded stub → draft, +1 source: full mechanism with motivation, Q/K/V, scaled dot-product, matrix form, permutation equivariance), [[ml_concepts/multi-head-attention]] (new, draft), [[ml_concepts/cross-attention]] (new, draft), [[ml_concepts/causal-masking]] (new, draft), [[methods/transformer]] (new, draft), [[topics/transformers]] (new, draft), [[ml_concepts/positional-encoding]] (+1 source, no body change), [[methods/sinusoidal-position-encoding]] (+1 source, no body change), [[index]] (updated).
- **Notes:** Opens the transformer area of the wiki via a new topic primer ([[topics/transformers]]) that connects existing PE pages with the new attention concepts. No contradictions with prior content. Did not create stubs for residual-connection, layer-normalization, position-wise-FFN, beam-search — they're mentioned but not characterised in the source; better filled by a focused ingest. Did not embed images: source uses external jalammar.github.io URLs, which the wiki avoids on rot-risk grounds — figures are described in prose. Two methodological questions surfaced (Pre-LN vs Post-LN; multi-head specialisation in modern LLMs) but kept as "Open questions" sections on concept pages rather than promoted to `questions/` — the source itself doesn't formulate them as research questions, just touches the territory. Reading queue on [[topics/transformers]] lists primary sources for natural next ingests (Vaswani 2017, Annotated Transformer, BERT, GPT-1, T5, ViT, FlashAttention).

## [2026-05-17] ingest | Attention Mechanisms (lecture on modern attention variants)

- **What:** Survey lecture on the modern attention landscape, organised around the KV-cache as the inference-time bottleneck. Covers the MHA → MQA → GQA → MLA spectrum (with Hugging Face ablations showing GQA-with-small-groups outperforms MHA), gated attention (sigmoid gate suppressing attention sinks), linear attention and Gated Linear Attention with hybrid softmax/SSM stacks (Mamba-2 in Nemotron-H/Falcon H1; DeltaNet in Qwen3-Next), and long-context patterns (sliding window attention, dual chunk attention, local/global interleaving, document masking). Includes concrete frontier-model adoption (Kimi-K2, DeepSeek, Trinity Large, gpt-oss-120b, SmolLM3, OLMo 3, Gemma 3, Qwen-2.5). Sourced from `raw/lectures/Attention Mechanisms.md`.
- **Pages touched:** [[sources/attention-mechanisms-lecture]] (new, draft), [[ml_concepts/kv-cache]] (new, draft), [[ml_concepts/multi-query-attention]] (new, draft), [[ml_concepts/grouped-query-attention]] (new, draft), [[ml_concepts/multi-latent-attention]] (new, draft), [[ml_concepts/gated-attention]] (new, draft), [[ml_concepts/linear-attention]] (new, draft, covers GLA and hybrid stacks), [[ml_concepts/sliding-window-attention]] (new, draft), [[ml_concepts/document-masking]] (new, draft), [[ml_concepts/attention-sink]] (new, stub), [[topics/attention-variants]] (new, draft — narrative primer organising the whole area), [[ml_concepts/multi-head-attention]] (+1 source, new KV-cache cost section, links to variants, open question on MQA/GQA replaced with the better-formulated "why GQA-small-group beats MHA"), [[topics/transformers]] (+1 source, rewrote "Methods that grow from these ideas" to point at the new primer and summarise variant families, updated open threads — the MQA/GQA loss thread is *resolved* with the surprising twist that GQA wins, hybrid stacks added), [[index]] (updated — Start here gets the new primer, ML concepts gets 9 new entries, Topics gets the primer, Sources gets the lecture).
- **Notes:** Opens a major new region of the wiki organised around inference economics. The single most impactful finding — GQA-with-small-groups outperforms MHA — resolves an existing open thread in [[topics/transformers]] that previously assumed GQA loses a little quality. Reformulated as "*why* does it win" — the lecture has no mechanistic explanation. Three ASCII diagrams from the source (KV-sharing spectrum, sliding-window vs causal full, document mask vs causal) copied verbatim onto the relevant concept pages. No external images to embed — the source is markdown-only. Did not create separate `questions/` pages; the lecture doesn't formulate research questions explicitly, and the Open questions sections on individual concept pages capture them well enough. Reading queue on [[topics/attention-variants]] lists primary sources for further ingests (Shazeer 2019 MQA, Ainslie 2023 GQA, DeepSeek-V2/V3 techrep, Mamba/Mamba-2, GLA, StreamingLLM, SmolLM3 techrep, Gemma 3 techrep).

## [2026-05-17] lint | wiki audit

- **Findings:** 2 blockers (broken links), 0 structural, 1 editorial (low-priority voice nit, deferred).
- **Fixed in this pass:** [[ml_concepts/flow-map]] (replaced broken `[[methods/consistency-model-sampling]]` reference with inline description of the fixed-schedule $\tau_0 < \ldots < \tau_K$ sampling protocol — no separate page warranted on current sources), [[sources/illustrated-transformer]] (removed literal `[[stub-link]]` placeholder text; rephrased as «stub-кандидаты»).
- **Deferred:** voice nit in [[ml_concepts/gated-attention]] («в данной позиции» → «на текущей позиции»); 5 ml_concept + 1 method stubs awaiting their dedicated source ingests.
- **Suggested next:** focused ingest on score-based diffusion would upgrade four stubs ([[ml_concepts/diffusion-model]], [[ml_concepts/score-function]], [[ml_concepts/probability-flow-ode]], [[ml_concepts/flow-matching]]) and bridge the variational-inference and few-step-generative-models regions.
